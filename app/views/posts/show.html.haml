%p#notice= notice

%main.show
  %h2.showh2=@post.title
  =@post.content

  .bottom_line
    = link_to 'Back', posts_path, class: 'button'
    -# = link_to 'Edit', edit_post_path(@post), class: 'button'
    -# = button_to 'Delete', @post, method: :delete, class: 'button', data: {confirm: "Are you sure?"}
